# Local (default, non-Vercel): SQLite file in prisma/dev.db
DATABASE_URL="file:./dev.db"

# Vercel + Supabase (set these in Vercel project env vars)
# DATABASE_URL should be the Supabase pooled URL (usually port 6543)
# DIRECT_URL should be the Supabase direct URL (usually port 5432)
DIRECT_URL=""

# App settings
APP_BASE_URL="http://localhost:3000"
RP_ID="localhost"
SECRET_COOKIE_PASSWORD="change-me-to-at-least-32-characters"
